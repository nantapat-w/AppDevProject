services:
  backend-api:
    build: ./backend
    ports:
      - "5000:5000"
    env_file: ./backend/.env  # ดึงค่าจาก .env มาใช้เลย

  frontend-web:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend-api